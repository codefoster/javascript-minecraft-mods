exports.greetThisPlayer = function() {
    echo('Hi, ' + self.name);
}

//TASK: allow the function to take a player so we can greet someone other than self
//TASK: combine these functions into one that defaults the player to self if not provided
//TASK: take two parameters - the name and the greeting